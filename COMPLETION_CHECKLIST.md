# Watchdog completion ledger

Owner: current Codex/Astra conversation. Base: 07d35f5. Implementation: interactive Devin, exact `--model swe-2`, no fallback. Understood as: atomize the remaining product work, launch at most two isolated workers, independently verify candidates, and progress until product evidence or a concrete authority boundary.

## Contracts

Each row is one assignment, one primary artifact, one principal oracle. Workers have at most two Todo items, no subagents, no commit/push/merge/tag/publish/deploy or worktree deletion. Allowed source paths are row-specific; build outputs belong inside that task worktree. Reports are terminal responses. Owner reviews scope, diff and oracle before accepting. A blocked task receives at most one narrowed retry on SWE-2. Preserve rejected diffs and evidence; rollback means omit the candidate from owner integration, never reset user files.

Evidence: L=source/build, S=synthetic test, R=real runtime, D=downloaded/installed distribution, H=human observation. Gates: none=local scope; QA=disposable runtime and OS permission interaction; ACCOUNT=external configuration; PUBLISH=explicit commit/push/merge/release permission; APPLE=optional paid distribution policy and credentials.

Existing baseline (not new work): identity authorization, stale gating, metrics, monotonic thresholds, bounded helpers, normalization and 79 passing unit tests from prior audit. They remain regression checks. Prior UI success was historical CI, not current local execution. No-paid unnotarized beta is the existing policy; notarization is NOT a mandatory blocker. Sandbox absence alone is not a defect for this process-control utility. Existing installer uses rename/backup but full crash-atomic recovery is unproven. Proposed soak limits are acceptance targets, not past measurements.

## Atomic queue

Every row begins pending. Completion requires the listed observable outcome and owner acceptance. Failure conditions reject the candidate; preserve it and leave dependent IDs blocked.

| ID / priority | Predecessors | Exact change scope → single artifact / target outcome | Principal verification / completion condition | Failure / rollback condition | Evidence / gate |
|---|---|---|---|---|---|
| A01 P0 ✅ accepted | none | ProcessMonitor.swift + ProcessMonitorTests.swift → orphan-only badge exclusion patch | Focused ProcessMonitorTests: orphan-only count=0; CPU/memory overlap count=1 | changed control semantics or failing oracle: omit patch | L,S / none |
| A02 P0 ✅ accepted | A01 | WatchdogMenuView.swift → attention empty-description patch | Source assertion: attention description names CPU/memory only | misleading orphan wording remains: omit patch | L / none |
| A03 P0 ✅ accepted | none | WatchdogPreviewApp.swift → explicit non-registering login controller dependency | WatchdogPreview Debug build succeeds | system registration introduced or build fails: omit patch | L / none |
| A04 P0 ✅ accepted | A03 | WatchdogSnapshot.swift → explicit non-registering login controller dependency | WatchdogSnapshot Debug build succeeds | registration introduced or build fails: omit patch | L / none |
| A05 P0 ✅ accepted | none | ProcessMonitor.swift → injectable cwd resolver boundary | Debug build accepts injected resolver while preserving default resolver | raw sample publication blocked: omit patch | L / none |
| A06a P0 ✅ accepted | A05 | ProcessMonitor.swift → refresh dispatches raw snapshots to cwd resolver | ProcessMonitorTests pass with generation-safe existing merge | missing refresh resolver call: omit patch | L,S / none |
| A06b P0 ✅ accepted | A06a | ProcessMonitor.swift → injectable worktree resolver boundary | Debug build accepts injected resolver while preserving default | missing default injection: omit patch | L / none |
| A06c P0 ✅ accepted | A06a,A06b | ProcessMonitor.swift → cwd merge triggers worktree lookup for the merged snapshot | ProcessMonitorTests pass with current-generation merge guard | lookup starts before cwd merge: omit patch | L,S / none |
| A06 P0 ✅ accepted | A05 | ProcessMonitor.swift + ProcessMonitorTests.swift → cwd result triggers worktree lookup for current generation | Integration oracle: current cwd result yields a clean worktree state; old generation remains rejected | stale merge or absent projectName: omit patch | L,S,R / none |
| A07 P0 ✅ accepted | none | WorktreeStateResolver.swift + WorktreeStateResolverTests.swift → clean label limited to observed Git status, not deletion safety | Table-driven clean/detached/no-upstream oracle never asserts safe removal | deletion-safety claim survives: omit patch | L,S / none |
| A08 P0 ✅ accepted | A07 | WorktreeStateResolver.swift + ProcessRow.swift + WorktreeStateResolverTests.swift → lookup failure represented as unavailable rather than missing | timeout fixture unavailable; absent path separately identified | command failure called missing: omit patch | L,S / none |
| A09 P0 ✅ accepted | A06,A08 | WorktreeStateResolver.swift + tests → bounded queue/in-flight delivery preserves pending paths | More-than-two-path oracle eventually delivers every bounded admitted result | starvation, unbounded queue or cancelled merge: omit patch | L,S / none |
| A10 P0 ✅ accepted | A01 | AutoSuspendPolicyTests.swift → auto-suspended process recovery visibility oracle | Sequence hot→SIGSTOP→suspended observation stays in all scope and actionability permits resume | stopped process becomes undiscoverable: reject; subsequent narrow fix required | S / none |
| A11 P0 — not needed | A10 | ProcessMonitor.swift + WatchdogMenuView.swift + tests → explicit suspended-process recovery list if A10 fails | A10 oracle passed: suspended process remains visible in all scope and manual resume-capable | automatic resume/kill introduced: omit patch | S / none |
| A12 P0 ✅ accepted | A03,A04 | Preview/Snapshot entrypoints + small fixture service → isolated defaults and fake login service | Both Debug schemes build with no-op login service | real permission/register mutation: omit patch | S / QA |
| A13 P0 partially evidenced | A01,A02,A06,A09,A11 | WatchdogUITests.swift + DEBUG fixtures → orphan/cwd row assertions | Fixture test source asserts orphan wording and cwd metadata; bundle builds, but shipping UI runner remains unverified | actual signals or reliance on Preview-only UI: reject | S / QA |
| A14 P0 ✅ accepted | A13 | QA helper source + project.yml → disposable heartbeat/TERM-marker executable | Helper builds; direct SIGTERM writes its marker and exits | uncontrolled descendants or no cleanup: omit artifact | L,R / none |
| A15mode P0 ✅ accepted | A14 | WatchdogApp.swift → DEBUG live-QA launch argument | Debug build accepts live monitor fixture host with login registration suppressed | mode leaks into Release: omit patch | L / none |
| A15qa P0 ✅ accepted | A15mode | project.yml + WatchdogLiveQA → unique-bundle real-monitor QA app | Debug target builds and Cua launches the installed unique-bundle QA app | target alters shipping app behavior: omit patch | L,R / none |
| A15 P0 partially evidenced | A14 | QA run evidence only → real menu launch and sampling receipt | Unique-bundle Debug QA app shows real system samples and matching helper PID; the earlier LaunchServices ambiguity is root-caused to stale same-identifier copies registered on this host and was cleared, so shipping-bundle launch is now reachable but still unverified | fixture data substituted or shipping launch unavailable: retain partial evidence only | R / QA |
| A16 P0 ✅ accepted | A15 | QA run evidence only → suspend/resume receipt | UI stop freezes heartbeat, UI resume advances it | unrelated PID touched or state unproved: reject run | R / QA |
| A17 P0 ✅ accepted | A16 | QA run evidence only → terminate receipt | UI confirmation→TERM marker→verified exit | signal delivery mistaken for exit: reject run | R / QA |
| A18 P0 ✅ accepted | A17 | QA run evidence only → force-quit/cancel receipt | Cancel preserves helper; separate confirmed kill exits fresh helper without TERM marker | missing explicit confirmation: reject run | R / QA |
| A19 P0 ✅ accepted (sizing/focus H-verified; confirmation clause recorded as residual) | A13 | QA evidence only → real menu sizing/focus-loss receipt | Fixed and confirmed by the user's own before/after screenshots: the pre-fix build (a Sep 2 Debug bundle in DerivedData) showed an empty list area and a truncated header, while the installed `9ca3770e…` candidate renders the empty state and real process rows in a 380×520 popover (AX: `AXScrollArea` 310 pt inside a 520 pt window, rows with PID/CPU/memory/actions). Root cause was MenuBarExtra sizing to the ScrollView's zero ideal height. The frontmost app is not disturbed. Residual, accepted by user decision: the confirmation-survives-close/reopen receipt was not obtained (needs the row's action menu; the agent path is unsafe on the live host — see the near-miss recorded in the evidence file). Evidence: `docs/evidence/a19-popover-20260921/VERDICT.md` | fixed-window fixture used as popover proof: reject run | R,H / QA |
| A20 P0 ✅ accepted | none | AGENTS.md, INSTALL.md, README.md → accurate ad-hoc/unnotarized beta contract | Assertions match inspected official artifact and no-paid policy | invented Developer ID requirement or false signing claim: omit patch | L,D / none |
| A21 P0 ✅ accepted | A20 | project.yml → Release artifact without get-task-allow | codesign entitlements inspection finds no debug permission | debug entitlement persists or signature invalid: reject artifact | L / none |
| A22 P0 ✅ accepted | A21 | Packaging script → ZIP/DMG and SHA/source manifest generator | Package verification binds version, SHA, architecture, checksum and signature | inconsistent source/artifact metadata: reject artifact | L,D / none |
| A23 P0 | A22,A18,A19 | QA evidence only → clean-account Gatekeeper approval receipt | Local installed candidate is signature-valid but `spctl --assess` rejects it and has no quarantine xattr; only a downloaded quarantined candidate launched through documented per-app approval can close this gate | quarantine removed/global bypass used: reject run | D,H / QA |
| A24 P0 ✅ accepted | A22 | Installer QA evidence → fresh --no-launch install receipt | Target bundle valid, version correct, no launched process | wrong bundle or launch occurs: reject run | D / QA |
| A25 P0 ✅ accepted | A24 | Installer QA evidence → graceful-update/refusal receipt | Running disposable prior install updates; TERM-resistant app is preserved | overwrite while running or automatic kill: reject run | D / QA |
| A26 P0 ✅ accepted | A25 | Installer QA evidence → failed replacement recovery receipt | Simulated staged replacement failure restores the prior bundle | old bundle lost: narrow installer repair before retry | D / QA |
| A27 P0 ✅ accepted | A22,A16 | Soak measurement script → bounded RSS/CPU/FD/helper sampler | Short run emits timestamped measurements bound to candidate SHA/hash | sensitive command lines or missing samples: reject artifact | L,R / none |
| A30 P0 ✅ accepted | A27 | scripts/analyze-soak.sh → timestamp-gap verdict | Reports largest adjacent timestamp gap and exits 4 when it exceeds two intervals | a discontinuous run accepted from count alone: reject receipt | L,S,R / none |
| A28 P0 partial stress evidence; continuity failed | A27,A23,A30 | Soak evidence → 8-hour candidate stability receipt | 2h30m36s candidate/helper survival, bounded 38–40 FD and post-sleep recovery were observed; actual 703-second Thermal Emergency Sleep gap fails the gap-free 8-hour receipt, and the run was then intentionally stopped | leak/gap/crash: stop dependent release and isolate failing condition | R,D / QA |
| A29 P0 partially evidenced | A28 | QA evidence → sleep/wake recovery receipt | Actual Thermal Emergency Sleep (2026-09-21 07:11:01–07:21:46 KST) preserved candidate/helper survival and later heartbeat, but fresh post-wake Watchdog UI observation remains unverified | stale authorization usable: release blocked | R / QA |
| B01 P1 ✅ source accepted, remote unverified | A03,A04 | ci.yml → Preview/Snapshot build jobs | Local workflow syntax and both scheme builds pass; remote candidate-SHA run needs push authority | jobs skipped or wrong SHA: reject gate | L / PUBLISH for remote run |
| B02 P1 ✅ accepted | A12 | Snapshot evidence → rendered PNG receipt | Two expected-size fresh PNGs render complete, unclipped content under visual inspection | blank/clipped output: reject receipt | S / none |
| B03 P1 ✅ source accepted, remote unverified | none | ci.yml → explicit Xcode/XcodeGen selection | Workflow logs selected `xcodebuild` and `xcodegen` versions; remote log needs push authority | unsupported or implicit version: omit patch | L / PUBLISH |
| B04 P1 ✅ accepted | none | ProcessMonitor.swift → async notification-settings query | Debug/Release build succeeds without notification callback-capture warning | changed permission behavior: omit patch | L,S / none |
| B05 P1 ✅ accepted | none | WatchdogApp.swift → remove unused/deprecated API use | Debug/Release build succeeds without those specific warnings | single-instance behavior altered: omit patch | L,S / none |
| B06 P1 ✅ source accepted, remote unverified | A22 | candidate.yml → candidate-only packaging workflow | Local YAML/source verification proves read-only candidate artifact workflow; remote run needs push authority | unexpected write/publish privilege: omit workflow | L,D / PUBLISH |
| B07 P1 | A23 | QA evidence → notifications denied/granted receipt | Real permission status and banner match UI | synthetic delivery counted as real: reject run | R,H / QA |
| B08 P1 | A23 | QA evidence → login registration/opt-out/relogin receipt | Installed candidate launches on login only when enabled | test host registered or opt-out lost: reject run | R,D,H / QA |
| B09 P1 | A19 | Human QA evidence → keyboard/VoiceOver destructive-action receipt | Participant finds/understands and cancels destructive action with keyboard | confusion or inaccessible control: narrow follow-up fix | H / human availability |
| B10 P1 — external setting absent | B01,B03,B06 | GitHub settings → required-check protection receipt | Read-only API reports main is unprotected; configuration requires repository-admin authority | unauthorized account change: do not apply | D / ACCOUNT |
| B11 P1 — external setting disabled | none | GitHub settings → secret-scanning receipt | Read-only API shows feature supported but disabled; enabling requires repository-admin authority | unavailable entitlement: record limitation | D / ACCOUNT |
| B12 P1 | A28,A29,B07,B08,B09,B06 | Release evidence manifest → candidate acceptance record | Every required receipt names same SHA/hash; no unresolved P0 | mismatched/missing evidence: no publish | L,S,R,D,H / none |
| B13 P1 | B12 | Remote release only → published beta artifact | Downloaded published hashes equal accepted manifest | mismatch: stop rollout and preserve prior release | D / PUBLISH |
| C01 P2 | B12 | ProcessMonitor logging only → redacted error diagnostics | Failure fixture logs reason without path/command secrets | sensitive output: omit patch | L,S / none |
| C02 P2 | B12 | OrphanClassifier + tests → one newly evidenced agent lineage case | Recorded case classified correctly without regressions | guessed support or false positives: omit case | L,S / none |
| C03 P2 | B12 | Performance evidence → 24h/high-process-count profile | Published latency/resource summary names workload and hardware | unsupported broad performance claim: reject report | R / QA |
| C04 P2 conditional | B12, explicit policy change | Signing script only → Developer ID/notarization/staple path | Accepted notarization + staple + post-staple spctl success | no credentials/rejection: retain no-paid beta path | D / APPLE,PUBLISH |

## DAG and execution waves

First wave: A01 and A03, each isolated from 07d35f5. Owner accepts each diff and reruns its focused oracle before integration. Wave 2: A02 and A04 on accepted owner contents. Then A05/A07; A06/A08; A09/A10; conditional A11/A12; A13 plus A20; A14 plus A21; A15-A18 sequential plus A22; A19/A23; A24-A26; A27-A29. B tasks may run alongside only where predecessors and file ownership allow. All runtime claims bind to the integrated candidate, never a worker's omitted snapshot. B12 joins all gates; B13 requires publication authority. C tasks do not block the existing no-paid beta objective.

## Acceptance ledger

A01, A02, A03, and A04 are accepted; see WAVE_01.md. A01 focused tests pass when DerivedData is outside the worktree. On 2026-09-21, the integrated owner snapshot also built with the default signed Release configuration at `/tmp/watchdog-release-signed-recheck-20260920`; `codesign --verify --deep --strict` passed and its entitlements contained no `get-task-allow`. A30 was accepted after the owner independently verified shellcheck, a 210-second synthetic gap rejection (exit 4), a contiguous 60-second fixture pass, and the running candidate's detected 703-second Thermal Emergency Sleep gap. Native goal is complete only after applicable product/runtime/distribution evidence passes; creating this file, launching workers or green unit tests alone cannot complete it. User-exclusive missing authority becomes an explicit boundary, not a claim of readiness.

### 2026-09-21 (둘째 세션) — 후보 재빌드, LaunchServices 원인 규명, soak 기동

- A21/A22 재확인: `scripts/build-candidate.sh`로 재빌드한 후보가 `0.2.0`/`5`, `x86_64 arm64`, adhoc 서명, `get_task_allow=false`, `codesign --verify --deep --strict` 통과.
- A22 보강: `manifest.json`이 HEAD만 기록해 dirty worktree 후보를 식별하지 못하던 문제를 수정했다. 이제 `source_tree`(작업 트리 해시)와 `source_dirty`를 함께 기록한다. 근거·검증은 `docs/evidence/session-20260921-candidate-and-launchservices.md`.
- A15 갱신: "LaunchServices ambiguity"의 원인은 **호스트에 남은 동일 bundle id 복사본의 등록**이었다. `launch_app`이 실제로 `.Watchdog.app.backup-20260901-144351`을 실행한 사례를 확인하고 stale 등록 100개를 해제했다. installer 백업 가설은 **재현 실패로 반증**되어 `scripts/install.sh`는 원상 복구했다. 위치 이전도 무효임을 확인(LaunchServices는 볼륨 전역을 인덱싱).
- A28/A29 진행 중: candidate pid `52429`(executable sha256 `6674f98d…`)와 helper pid `55385`에 대해 `2026-09-21T08:42:07Z`부터 28800s / 60s 간격 soak을 기동했다. 판정은 완료 후 `scripts/analyze-soak.sh`로 한다.
- A19/A23/B07/B08/B09: GUI 조작이 필요해 사용자가 **soak 종료 후 진행**으로 결정했다. B08은 실제 로그아웃이 필요해 에이전트 수행 불가로 남는다.
- **A19 P0 실패(2026-09-21)**: 설치된 후보의 실제 MenuBarExtra popover를 관찰했다. 창은 298×212로 열리고 크롬·포커스 비탈취·닫기/재열기·상태 보존은 정상이지만, **프로세스 목록이 사용 불가**다. 목록 영역이 약 8pt 슬리버로만 그려지고, AX는 행 그룹을 y=210..258에 배치하는데 창 하단은 y=247이며 푸터(224..237)와 겹친다. 창 높이가 내용(행 유무)을 따르지 않고 항상 212pt다. DEBUG UI 테스트가 `NSWindow(contentRect: 480×590)` fixture를 쓰기 때문에 이 결함이 테스트에 걸리지 않는다. 상세 근거: `docs/evidence/a19-popover-20260921/VERDICT.md`. **수정 없이는 릴리스 불가.**
- **A19 수정(2026-09-21, 같은 세션)**: 원인은 `MenuBarExtra`가 콘텐츠의 ideal height로 창을 맞추는데 목록이 `ScrollView`라 ideal height가 사실상 0인 것이었다. `WatchdogApp.swift`의 `MenuBarExtra` 콘텐츠에만 `.frame(width: 380, height: 520)`을 적용했다(`WatchdogMenuView` 본문은 UI 테스트 fixture와 공유되므로 건드리지 않았다). 설치된 빌드에서 측정: 창 298×212 → 380×520, 목록 영역 약 30pt → **337pt**, 모든 요소가 창 안에 위치. 새 후보 exe sha256 `9ca3770e…`.
- **A28 재시작(2026-09-21T10:05:50Z)**: A19 수정으로 후보 바이너리가 바뀌어(B12가 receipt를 단일 해시에 묶으므로) 이전 soak(1h18m)을 중단하고 고친 후보로 다시 시작했다. candidate pid `61242`(exe `9ca3770e…`), helper pid `64727`, 출력 `/tmp/wd-soak2-20260921`, 종료 예정 `18:05:50Z`. 첫 sample fd 53.
- **호스트 재부팅으로 그 run 무효(2026-09-22 08:11:28 KST)**: 머신이 재부팅되면서 soak 프로세스와 `/tmp` 산출물이 모두 사라졌다. A28은 완주 기록이 없으므로 폐기한다.
- **A19 수정 사람 검증(2026-09-22 08:22 KST, H 등급)**: 사용자가 실제 클릭으로 캡처를 제공했다. 수정 전 캡처는 DerivedData의 9월 2일 Debug 빌드(pid 14058)에서 나온 것으로, 목록 영역이 완전히 비어 있고 헤더 설명이 잘려 있었다. 수정 후 캡처는 설치된 후보(`9ca3770e…`, pid 34773)에서 380×520 창에 빈 상태(`확인할 프로세스가 없습니다` + 설명)가 온전히 렌더됨을 보여준다. A19의 크기·사용가능성 항목은 통과. 남은 하위 항목은 confirmation survives close/reopen 하나다.
- **환경 제약 기록**: 디스플레이 2개(메인 1728×1117 @ 0,0, 보조 1920×1080 @ −84,−1080), 재부팅 후 디스플레이 슬립 및 메뉴바 숨김, `cua-driver` desktop 스코프가 메인 디스플레이만 지원하는 조합 때문에 에이전트가 status item을 안정적으로 클릭할 수 없다. AXPress로 연 popover는 앱이 활성화되지 않아 1초 내 닫히고 콘텐츠가 실체화되지 않는다. 남은 GUI 항목(A19 확인창, A23, B07, B09)은 사람 손이 필요하다.
- **A28 재개(2026-09-21T23:20:58Z = KST 08:20:58)**: 고친 후보로 다시 시작. candidate pid `34773`(exe `9ca3770e…`), helper pid `48967`, 출력 `/tmp/wd-soak-20260922`, 종료 예정 `2026-09-22T07:20:58Z`(KST 16:20:58).
- **A19 종료(2026-09-22)**: 크기·사용가능성·포커스 항목 통과(H, 사용자 캡처 + AX 교차 확인). 목록 행이 `AXScrollArea`(310pt) 안에 정상 배치됨을 확인. 헤더/필터/스코프 전환/AXPress로 popover 열기까지 동작. 미확보 항목(confirmation survives close/reopen)은 사용자 결정으로 잔여 위험으로 기록하고 닫았다.
- **근접 사고 기록(2026-09-22)**: 확인창을 열려던 에이전트 스크립트가 `AXMenuItem`을 부분일치로 찾으면서 Apple 메뉴의 `시스템 종료…`에 매칭돼 시스템 종료 다이얼로그를 열었다(사용자가 취소, 확정·신호 전송 없음, `/sbin/shutdown` 예약 없음, 표적 프로세스 생존 확인). 금지·필수 가드레일을 `docs/evidence/a19-popover-20260921/VERDICT.md`에 기록했다. 이후 GUI 확인창 검증은 중단했다.
