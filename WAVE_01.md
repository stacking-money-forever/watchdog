# Wave 01 launch contract

Owner base 07d35f5; parent w5C:p1. Model exact `devin --model swe-2 --permission-mode dangerous`; Meter provider uncovered, no provider alias guessed. `devin models list` timed out; live task will establish availability, not model catalogue claims. No fallback. Workers were later terminated by user instruction; no worker result was accepted without owner diff and oracle inspection.

| Task | Worktree | Pane | Scope | Single artifact | Oracle |
|---|---|---|---|---|---|
| A01 | /Users/justn/dev/.worktrees/watchdog-a01 | w5C:p2 | ProcessMonitor.swift, ProcessMonitorTests.swift | orphan-only count exclusion diff | ProcessMonitorTests pass, orphan-only zero and overlap one |
| A03 | /Users/justn/dev/.worktrees/watchdog-a03 | w5C:p3 | WatchdogPreviewApp.swift | non-registering login-controller dependency diff | WatchdogPreview Debug build |

Placement: place_new_pane, parent right 35%, next worker down 50% in side. Both --no-focus. Worktree-created workspace shells are not worker panes. Workers must not mutate other repositories or global settings, commit/push/merge/publish, delete worktrees, delegate, or create more than two Todos. One narrowed retry maximum. Owner accepts only after diff/oracle inspection.

Launch status: both interactive sessions ready, argv includes exact `--model swe-2`, both native TUI footers show `SWE-2 High`. Requests submitted and Thinking observed. Devin reports sandbox implies autonomous mode and ignores accept-edits; sandbox workspace write boundary remains active. No configurable effort was requested or synthesized. Acceptance: pending.

## Owner decisions

- A03 ACCEPTED for the narrow constructor/build repair. One-file diff reviewed, `git diff --check` passed. Owner command `xcodebuild -quiet -project Watchdog.xcodeproj -scheme WatchdogPreview -configuration Debug -derivedDataPath DerivedData/owner-a03 build` in watchdog-a03 exited 0. Generated Preview app passed `codesign --verify --deep --strict --verbose=2`. Log: watchdog-a03/DerivedData/owner-a03.log. Identical file integrated into owner worktree via apply_patch and cmp verified. Does not prove runtime isolation; A12 remains pending. Worker sandbox build exited 65 due nested sandbox/Swift macro expansion; worker reports it ran twice, so no further worker retry allowed for A03.
- A01 ACCEPTED after a retry with DerivedData outside the worktree: `/usr/bin/xcodebuild ... -derivedDataPath /tmp/watchdog-a01-verify.RjePWa test -only-testing:WatchdogTests/ProcessMonitorTests` passed 23/23. The original worktree-local failure remains recorded as an environment issue, not a source failure. Candidate changes only ProcessMonitor.swift and ProcessMonitorTests.swift, replacing orphan union and adding orphan-only regression. Owner diff inspection and cmp passed. The worktree-local failure is not a source test result; the /tmp test is the acceptance oracle.
- A02 ACCEPTED: one-line attention empty-state copy now describes sustained CPU/memory processes only. Owner checked it against `ProcessMonitor.visibleProcesses`, `rg`, and `git diff --check`; candidate scope was one file.
- A04 ACCEPTED: Snapshot owns a non-registering `LaunchAtLoginController` and passes it to `WatchdogMenuView`. Worker performed `/tmp/watchdog-a04-build` Debug build successfully; owner integrated the exact three-line diff. Runtime isolation remains A12.
- Owner independently rebuilt A04 in `/tmp/watchdog-a04-verify.A5XUx6`; `WatchdogSnapshot` Debug build succeeded.
- A07 ACCEPTED after the one permitted narrowed retry: wording now identifies an observed clean Git status without a deletion-safety promise. Owner ran WorktreeStateResolverTests in `/tmp/watchdog-a07-verify.Y02b0Y`: 7/7 passed. The worker's broad `rg` found an unrelated README phrase; that file was out of scope and unchanged.
- A05 ACCEPTED: ProcessMonitor now accepts a `WorkingDirectoryResolver` while retaining the default. Worker and owner `/tmp` Debug builds succeeded; owner result is `/tmp/watchdog-a05-verify.pjNGeK`. Resolver invocation itself remains A06.
- A08 ACCEPTED: absent filesystem paths now map to `.missing`; failed or non-git lookup maps to `.unavailable`; the row uses a neutral unavailable badge. Owner reran 8 focused resolver tests in `/tmp/watchdog-a08-owner.6kEK4y`; all passed. A07's observed-clean-status wording remained intact during delta integration.
- A06a ACCEPTED by owner bounded recovery after the worker ended without a source artifact: `refresh()` now dispatches raw snapshots and current generation to `workingDirectoryResolver`, merging only through the existing generation-safe method. ProcessMonitorTests passed 23/23 in `/tmp/watchdog-a06-owner.iygRO4`. A direct refresh-to-resolver invocation test still needs a sampler seam; subsequent worktree-state chaining remains separate.
- A09 ACCEPTED by owner bounded recovery after the worker exhausted its retry without source output: active-limit overflow is now bounded FIFO queueing (capacity 16), cancelled queues clear, and test drain follows queued work. Owner resolver tests passed 9/9 in `/tmp/watchdog-a09-owner.dAIyPf`.
- A10 ACCEPTED by owner bounded recovery after the worker ended without an artifact: auto-suspend emits SIGSTOP, then a following suspended observation remains visible in all scope with fresh resume-capable actionability. AutoSuspendPolicyTests passed 5/5 in `/tmp/watchdog-a10-owner.Y4Mahw`.
- A06b ACCEPTED by owner bounded recovery after the worker ended without running its build: ProcessMonitor now accepts a defaulted WorktreeStateResolver. Owner Debug build succeeded in `/tmp/watchdog-a06b-owner.bBzK2j`.
- A06c ACCEPTED by owner bounded recovery after the worker ended before its trigger: current-generation cwd merge now starts worktree resolution for the merged snapshot only. ProcessMonitorTests passed 23/23 in `/tmp/watchdog-a06c-owner.ZWmekv`.
- A06 ACCEPTED after owner added the direct chain oracle: an injected resolver reports `# branch.head main` for the current merged cwd, and the monitor records `.clean` with detail `main`; stale cwd merging remains separately rejected. Standard `xcodebuild test` stalls in this host's automation runner, but the built unit bundle was executed directly after a temporary rpath symlink in its build output: 84/84 passed, including the new chain test (0.033s).
- A12 ACCEPTED after owner completed the worker's valid partial entrypoint diff: Preview/Snapshot each use a private no-op LaunchAtLoginServicing and automaticallyRegister false. Owner builds succeeded in `/tmp/watchdog-a12-preview-owner.3mUW5g` and `/tmp/watchdog-a12-snapshot-owner.PcPiix`.
- A14 ACCEPTED by owner bounded recovery after worker termination: WatchdogE2EHelper target uses caller paths only, periodic atomic heartbeat, and SIGTERM marker handling. XcodeGen regenerated the project; Debug build passed in `/tmp/watchdog-a14-owner-final2.VkeQqN`. Direct runtime contract passed: heartbeat advanced and a SIGTERM wrote `term` then exited 0.
- A20 ACCEPTED: artifact recheck reported `Signature=adhoc`, `TeamIdentifier=not set`, and `spctl` rejection; AGENTS, README, and INSTALL now state the same no-paid public-beta distribution facts.
- A24 ACCEPTED by owner direct temp install: `scripts/install.sh --install-dir /tmp/watchdog-install-owner.EiEan5 --no-launch` downloaded the pinned v0.2.0 ZIP, verified checksum and structural signature, installed bundle ID `dev.justn.watchdog` version `0.2.0`, and left zero text references to the temporary executable open.
- A21 ACCEPTED: Release-only CODE_SIGN_INJECT_BASE_ENTITLEMENTS is disabled and XcodeGen regenerated the project. Owner Release build `/tmp/watchdog-a21-owner.V5NDQa` succeeded; codesign entitlement output contains no get-task-allow.
- Candidate GUI launch attempt: Cua hidden launch with bundle ID and `--ui-test-live` produced PID 93279 but lsof showed `/Users/justn/Applications/.Watchdog.app.backup-20260901-144351`, not the candidate Debug bundle. It exposed no Cua window, so window-scoped quit was refused. The exact agent-launched PID was gracefully SIGTERM-stopped. No candidate UI/runtime claim is accepted; LaunchServices ambiguity remains a QA-environment blocker.
- LiveQA build attempt: unique-bundle WatchdogLiveQA built in `/tmp/watchdog-liveqa-owner.vmA9fT`; Cua launch returned APP_NOT_INSTALLED even after local LaunchServices registration because its resolver does not accept /tmp app bundles. No QA app window or candidate sampling claim was produced. Installing a QA bundle in a recognized applications directory is an external-state boundary.
- A15mode ACCEPTED by owner bounded recovery after worker termination: DEBUG `--ui-test-live` is recognized as a fixture for lock/login suppression, opens the existing QA window host, and starts real monitoring. Debug build succeeded in `/tmp/watchdog-a15mode-owner.VQ2ppw`.
- A22 ACCEPTED by owner bounded recovery after worker termination: `scripts/build-candidate.sh` produced a local candidate at `/tmp/watchdog-candidate-smoke.e5cCYj/artifacts`. Manifest binds source `07d35f5`, v0.2.0 build 5, universal architectures, artifact checksums, ad-hoc/no-Team-ID signature and get-task-allow false; DMG and app structural verification passed.
- Owner deduplicated the accidentally repeated `WatchdogLiveQA` target and scheme in the XcodeGen source of truth. `xcodegen generate` plus Debug LiveQA build succeeded at `/tmp/watchdog-liveqa-dedupe.tQL9Tw`.
- B04/B05 ACCEPTED by owner bounded recovery: notification status now uses the native async API with a weak task capture; instance activation drops the deprecated ignored-other-apps option and the unused fixture value is removed. `build-for-testing` Debug plus Release build succeeded at `/tmp/watchdog-warning-clean.csrLlu`; its directly executed unit bundle passed 84/84. The new target code removed the corresponding Watchdog/ProcessMonitor warnings; remaining warnings are pre-existing test awaits and host CoreDevice plug-in drift.
- LiveQA runtime receipt: the installed unique-bundle DEBUG QA app sampled real host processes and displayed live worktree verdicts. With a Herdr-pane-owned disposable `WatchdogE2EHelper` PID 55631, exact Cua row identity was checked before each action. UI suspend produced process state `T` and a stable heartbeat `93 → 93`; UI resume produced state `S` and heartbeat advance to `99`; terminate confirmation named that helper and confirmation wrote `term` before verified exit. The helper pane was re-read as an idle zsh, then closed by exact ID `w5C:pQ`. The QA app was subsequently quit; its PID no longer existed. This proves the isolated DEBUG runtime control path, not a shipping-bundle launch or human usability claim.
- A18 ACCEPTED: a second unique-bundle DEBUG runtime used disposable helper PID 36439. The force-quit confirmation explicitly named the helper and SIGKILL consequence. Cancel left the helper alive, heartbeat advancing, and `term3.txt` absent. A separate confirmation exited that exact PID with `term3.txt` still absent. Pane `w5C:pR` was re-read as idle zsh and closed by exact ID; the QA app PID was then absent. This is isolated DEBUG runtime evidence, not shipping-bundle or human-observation evidence.
- A25 PARTIAL: a Cua-launched, unique-bundle QA copy at `/Users/justn/Applications/WatchdogInstallerQA/Watchdog.app` was identified by the installer's exact `lsof` text path. A second `--no-launch` install emitted “Stopping the installed Watchdog gracefully…”, old PID 89093 exited, and the replacement was a valid `dev.justn.watchdog` v0.2.0 bundle with no retained backup. The TERM-resistant refusal case remains unverified: SIGSTOP does not resist default SIGTERM on this macOS host. Installer hardening now uses an exact `ps comm` fallback when `lsof` cannot see a stopped process and uses `kill -0` for already-bound PIDs during the wait, failing conservatively if any remain alive. `bash -n` and fresh no-launch install smoke passed at `/tmp/watchdog-installer-script-smoke.IN9Kj7`.
- A26 ACCEPTED: in a dedicated temp install root `/private/tmp/watchdog-installer-recovery.WXdSX3`, a temporary PATH wrapper failed only the staged `Watchdog.app` → exact target move. Installer exited 1 with “Installation failed; the previous app was restored.” Target existed, binary SHA-256 remained unchanged, no backup remained, and structural code-signature verification passed. The wrapper is a temporary QA artifact outside the repository.
- A27 ACCEPTED: `scripts/soak-candidate.sh` validates a running PID against the exact candidate app text executable, structurally verifies its signature, caps duration at 8 hours and samples at 500, and writes only version/build/executable SHA plus RSS/CPU/FD/optional heartbeat values (never command lines). A 3-second Cua-launched QA candidate run with a Herdr-owned disposable heartbeat helper emitted four timestamped samples and metadata bound to SHA `b327f5…` at `/tmp/watchdog-soak-smoke.3uZb3I`. Helper PID 52315 received TERM, wrote its marker, became an idle shell, and its exact pane `w5C:pS` was closed; QA app PID 51089 was also absent afterward.
- A28 RUNNING: Cua launched a unique-bundle QA candidate PID 62688; helper PID 63898 is bound to `/tmp/watchdog-soak8h-heartbeat.txt`; exact Herdr pane `w5C:pV` is running `soak-candidate.sh` for 28800 seconds with 60-second cadence into `/tmp/watchdog-soak-8h-20260921`. First sample and manifest were observed. Retain panes `w5C:pT` and `w5C:pV` until terminal evidence is reviewed; do not interpret startup as completion.
- A25 ACCEPTED: the normal graceful-update receipt remains as above. For TERM-resistant refusal, a disposable minimal executable that ignores SIGTERM occupied the exact target path in `/private/tmp/watchdog-installer-resistant.VNHaoe/Watchdog.app`. Installer detected it, exited 1 with “Watchdog is still running”, retained the same target binary SHA with no backup, and never used SIGKILL. The fake PID 75262 was later stopped by SIGINT; its exact pane `w5C:pW` was verified idle and closed. This validates the installer safety branch without altering a user install.
- A11 NOT NEEDED: it is explicitly conditional on A10 failing. A10's synthetic stopped-observation sequence passed, and the subsequent live suspend/resume E2E proved the visible all-scope recovery path. No redundant fallback UI was added.
- A13 PARTIAL: fixture UI test now asserts `watchdog-fixture` cwd/project metadata alongside the existing orphan badge/reason assertions, and `build-for-testing` succeeded at `/tmp/watchdog-uitest-build.4FkKmo`. Direct `xctest` execution cannot supply the mandatory target-application XCTest configuration; normal `xcodebuild test` remains blocked by this host's automation enablement timeout. No UI-suite pass claim is made.
- B02 ACCEPTED: Snapshot initially exposed a stale fixed fixture timestamp; switching to `Date()` exposed an incomplete cache capture. Snapshot now forces display after layout. Two consecutive 960×1080 PNGs built and rendered from `/tmp/watchdog-snapshot-display.XykfTo`; visual inspection found complete dark UI, expected alert rows, current “방금 관찰” text, and no clipping/blank canvas.
- B06 SOURCE ACCEPTED: `.github/workflows/candidate.yml` is dispatch-only with `contents: read`, builds `scripts/build-candidate.sh`, verifies manifest/checksums/app executable, and uploads a 14-day Actions artifact. It has no release/publish API, write token, or deployment step. Ruby YAML parsing and source-policy search passed locally. A remote run remains intentionally unverified because push is authorization-gated.
- B01/B03 SOURCE ACCEPTED: CI now logs `xcodebuild -version` and `xcodegen --version`, then builds WatchdogPreview and WatchdogSnapshot in addition to existing unit/UI/Debug/Release checks. YAML parsing and local builds of both schemes passed at `/tmp/watchdog-ci-schemes.t0vlcw` (Xcode 27.0 build 27A266a; XcodeGen 2.45.4). A remote run remains push-gated.
- Integrated verification: Debug `build-for-testing` and Release build at `/tmp/watchdog-integrated-final.5QCOfv` succeeded. The built unit bundle was then directly executed with its local debug dylib rpath bridge: 84/84 passed. Release `codesign --verify --deep --strict` passed and entitlement inspection found no `get-task-allow`. UI test execution remains separately unverified because its host automation runner is unavailable.
- B11 READ-ONLY AUDIT: `gh api repos/stacking-money-forever/watchdog` reports this public repository supports `security_and_analysis`, but secret scanning, non-provider patterns, push protection, and validity checks are all `disabled`. No repository setting was changed; enabling is an account-authorized external action.
- B10 READ-ONLY AUDIT: `gh api repos/stacking-money-forever/watchdog/branches/main/protection` returns HTTP 404 “Branch not protected.” No branch policy was changed; required-check protection is an account-authorized external action.
- Script static verification: `shellcheck scripts/install.sh scripts/build-candidate.sh scripts/soak-candidate.sh` exited 0; `git diff --check` also passed.
- A29 PARTIAL: Cua-launched unique LiveQA PID 47075 showed fresh sampling, then was SIGSTOPped for 6 seconds and resumed. Fresh Cua state after resume showed new live system metrics and “방금 관찰 · 05:32:56”; the QA PID was then absent after quit. This exercises an app observation gap, but does not claim actual system sleep/wake behavior or replace its required human/runtime receipt.
- A28 analysis preparation: `scripts/analyze-soak.sh` now validates exact sample/metadata shape and completion count, verifies strictly increasing heartbeat, and emits candidate identity plus first/last/max RSS and FD summary without touching the candidate. ShellCheck passed; the completed 3-second smoke set produced 4/4 samples with FD 38→38 and heartbeat 12→16 at `/tmp/watchdog-soak-analysis-smoke.0Ck1pB`. It will be run against the 8-hour receipt only after its sampler exits.
- Both interactive Devin processes returned to shell. No model substitution, commit, push, merge or release occurred. Native goal remains active.
